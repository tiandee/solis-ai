# 实施方案 v5: Pseudo Labeling (伪标签) - 冲刺 < 400

## 背景与结论
- **v2 (Ensemble)**: 445.68
- **v3 (Stacking)**: 445.64
- **v4 (Direct MAE)**: ~496 (Failed)
- **v5 (Pseudo Labeling)**: **442.99** (New Best!) ✅

## 什么是 Pseudo Labeling
(原有内容保持不变...)

...

## 预期目标
这一策略通常能在比赛后期带来 3-5 分的稳定提升。目标是将分数从 445 推进到 **440** 左右。

---

## 结论 (2026-02-13) ✅

### 实验结果
- **训练过程**:
    - **LightGBM**: CV MAE 降至 **466.19** (v2: ~490)。
    - **XGBoost**: CV MAE 降至 **471.73** (v2: ~485)。
    - **CatBoost**: CV MAE 降至 **463.44** (v2: ~482)。
- **线上成绩 (LB)**:
    - 提交文件: `predictions_pseudo_v5.csv`
    - 分数: **442.9929**
    - 提升: 相比 v2/v3 提升了约 **2.7 分**。

### 总结
Pseudo Labeling 策略成功打破了 445 分的瓶颈。通过引入 LB 445 的高置信度预测作为伪标签，模型成功学到了更强的泛化能力。虽然距离 400 分还有差距，但方向是正确的。

### 下一步建议 (v6)
- **多轮伪标签 (Iterative Pseudo Labeling)**: 使用现在的 LB 443 结果再次作为伪标签，进行第二轮训练。
- **特征工程深挖**: 目前特征工程主要复用 v2，可能需要针对高价车挖掘更多特征。
